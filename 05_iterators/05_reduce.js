const myNums=[1,2,3,4]
const initial_value=0

const newNums=myNums.reduce((acc,cv) => acc+cv,initial_value)
console.log(newNums);

//reduce is basically use to do the sum of all the elements in object.